CREATE TABLE article (
    id int AUTO_INCREMENT,
    title varchar(255),
    url varchar(255),
    provider varchar(255),
    created_date datetime DEFAULT NOW(),
    PRIMARY KEY (id),
    UNIQUE(url)
)